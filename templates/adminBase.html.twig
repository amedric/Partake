{% extends 'base.html.twig' %}

{% block title %}Admin page{% endblock %}

{% block body %}

    <div class="ptk-adminBlock">

        <div class="ptk-adminContainer">

            <div class="ptk-adminSideMenuContainer">
                <a href="{{ path('app_user_new') }}" class="ptk-SideMenuLink">Add User</a>
                <a href="{{ path('app_user_index') }}" class="ptk-SideMenuLink">Users</a>
                <a href="{{ path('app_admin_project_list') }}" class="ptk-SideMenuLink">Projects</a>
                <a href="{{ path('app_category_index') }}" class="ptk-SideMenuLink">Teams</a>
            </div>

            <div class="ptk-adminViewContainer">
                <div class="ptk-adminTitleContainer">
                    {% block adminTitle %}{% endblock %}
                </div>
                {% block adminBody %}{% endblock %}
            </div>

        </div>
    </div>

    {#    <a href="{{ path('app_user_new') }}">Create new</a>#}
{% endblock %}
